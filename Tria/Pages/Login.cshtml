@page
@model Tria.Pages.LoginModel
@using Tria.Services
@inject IUiLocalizer T
@{
    ViewData["Title"] = T["Login_Title"];
    Layout = null;
    var lang = System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName;
}

<!DOCTYPE html>
<html lang="@lang">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] – Tria</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background-color: #f3f4f6; color: #111827; }
        .page { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 24px; }
        .window { width: 100%; max-width: 420px; background-color: #fff; border: 1px solid #d1d5db; border-radius: 8px; box-shadow: 0 10px 30px rgba(0,0,0,0.08); }
        .window-header { display: flex; align-items: center; justify-content: space-between; padding: 8px 10px; border-bottom: 1px solid #e5e7eb; background-color: #f9fafb; }
        .window-title { font-size: 12px; color: #6b7280; }
        .window-body { padding: 20px 22px 18px; }
        h1 { font-size: 20px; margin-bottom: 4px; }
        .subtitle { font-size: 13px; color: #6b7280; margin-bottom: 18px; }
        .field { margin-bottom: 14px; }
        .field-label { font-size: 11px; text-transform: uppercase; letter-spacing: 0.05em; color: #6b7280; margin-bottom: 4px; display: block; }
        .field-input { width: 100%; padding: 8px 9px; font-size: 14px; border-radius: 4px; border: 1px solid #d1d5db; outline: none; }
        .field-input:focus { border-color: #4b5563; }
        .validation-summary, .field-error { font-size: 12px; color: #b91c1c; margin-bottom: 8px; }
        .field-error { margin-top: 3px; }
        .btn-primary { width: 100%; padding: 8px 0; font-size: 14px; border-radius: 4px; border: 1px solid #111827; background-color: #111827; color: #f9fafb; cursor: pointer; }
        .btn-primary:hover { background-color: #374151; }
        .link-block { margin-top: 10px; text-align: center; font-size: 13px; }
        .link-block a { color: #111827; text-decoration: none; }
        .link-block a:hover { text-decoration: underline; }
        .guest-block { margin-top: 10px; padding: 8px 10px; border-radius: 4px; background-color: #e5f2ff; text-align: center; font-size: 13px; }
        .guest-button { border: none; background: none; color: #1d4ed8; cursor: pointer; padding: 0; }
        .guest-button:hover { text-decoration: underline; }
        .footer-note { margin-top: 12px; font-size: 11px; text-align: center; color: #9ca3af; }

        .lang-switcher { position: relative; display: inline-block; }
        .lang-btn { display: flex; align-items: center; gap: 4px; padding: 3px 8px; font-size: 13px; border: 1px solid #d1d5db; border-radius: 4px; background: #fff; cursor: pointer; color: #374151; }
        .lang-btn .arrow { font-size: 9px; color: #9ca3af; }
        .lang-dropdown { display: none; position: absolute; right: 0; top: calc(100% + 4px); background: #fff; border: 1px solid #d1d5db; border-radius: 6px; box-shadow: 0 6px 16px rgba(0,0,0,0.10); min-width: 150px; z-index: 100; overflow: hidden; }
        .lang-dropdown.open { display: block; }
        .lang-option { display: block; text-decoration: none; width: 100%; padding: 7px 12px; font-size: 13px; text-align: left; background: none; border: none; cursor: pointer; color: #111827; }
        .lang-option:hover { background: #f3f4f6; }
        .lang-option.active { background: #eff6ff; color: #1d4ed8; font-weight: 500; }
    </style>
</head>
<script>
function toggleLang(e) {
    e.stopPropagation();
    document.querySelector('.lang-dropdown').classList.toggle('open');
}
document.addEventListener('click', function() {
    document.querySelector('.lang-dropdown').classList.remove('open');
});
</script>
<body>
    <div class="page">
        <div class="window">
            <div class="window-header">
                <div class="window-title">@T["Login_Title"]</div>
                <div class="lang-switcher">
                    <button type="button" class="lang-btn" onclick="toggleLang(event)">
                        @(lang == "ru" ? "🇷🇺" : "🇬🇧") <span class="arrow">▼</span>
                    </button>
                    <div class="lang-dropdown">
                        <a href="/set-language?culture=en&returnUrl=/Login" class="lang-option @(lang == "en" ? "active" : "")">🇬🇧 English</a>
                        <a href="/set-language?culture=ru&returnUrl=/Login" class="lang-option @(lang == "ru" ? "active" : "")">🇷🇺 Русский</a>
                    </div>
                </div>
            </div>
            <div class="window-body">
                <h1>@T["Login_Heading"]</h1>
                <p class="subtitle">@T["Login_Subtitle"]</p>

                <div asp-validation-summary="All" class="validation-summary"></div>

                <form method="post">
                    @Html.AntiForgeryToken()
                    <div class="field">
                        <label asp-for="Input.Email" class="field-label">@T["Login_Email"]</label>
                        <input asp-for="Input.Email" autocomplete="email" class="field-input" />
                        <span asp-validation-for="Input.Email" class="field-error"></span>
                    </div>
                    <div class="field">
                        <label asp-for="Input.Password" class="field-label">@T["Login_Password"]</label>
                        <input asp-for="Input.Password" type="password" autocomplete="current-password" class="field-input" />
                        <span asp-validation-for="Input.Password" class="field-error"></span>
                    </div>
                    <button type="submit" class="btn-primary">@T["Login_Submit"]</button>
                </form>

                <div class="link-block">
                    @T["Login_NoAccount"] <a asp-page="./Register">@T["Login_Register"]</a>
                </div>

                <form method="post" asp-page-handler="Guest" class="guest-block">
                    <button type="submit" class="guest-button">@T["Login_Guest"]</button>
                </form>

                <div class="footer-note">@T["Login_Footer"]</div>
            </div>
        </div>
    </div>
</body>
</html>
